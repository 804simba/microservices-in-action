server:
  port: 9296
spring:
  application:
    name: CONFIG_SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/timolisa/config-server
          clone-on-start: true
